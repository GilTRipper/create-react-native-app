# create-react-native-app 🚀

## 🎉 Готово! Твой CLI инструмент создан!

Я создал полностью рабочий CLI инструмент для создания React Native приложений с твоим текущим стеком технологий.

## 📦 Что было создано

### 1. CLI Инструмент (`create-react-native-app`)
- ✅ Интерактивные промпты для создания проекта
- ✅ Поддержка npm, yarn, pnpm
- ✅ Автоматическая установка зависимостей
- ✅ Автоматическая установка iOS pods
- ✅ Инициализация git репозитория
- ✅ Замена всех плейсхолдеров на название проекта

### 2. Шаблон приложения
Полная копия твоего текущего проекта с:
- 🔥 Firebase (Analytics, Messaging, Remote Config)
- 🗺️ Google Maps с направлениями
- 🧭 React Navigation (Stack + Drawer)
- 💾 Zustand + TanStack Query + MMKV
- 🎨 React Native Reanimated
- 📱 Все твои библиотеки и настройки
- 🎭 Кастомные шрифты и иконки
- 📦 И всё остальное из твоего проекта!

## 🚀 Как использовать СЕЙЧАС

### Шаг 1: Установи зависимости

```bash
cd /path/to/create-react-native-app
npm install
```

### Шаг 2: Подключи CLI локально

```bash
npm link
```

Теперь команда `create-react-native-app` доступна глобально!

### Шаг 3: Создай тестовое приложение

```bash
# Перейди куда хочешь создать приложение
cd ~/Desktop

# Запусти команду!
create-react-native-app МоёПриложение
```

Ты увидишь вопросы:
- **Bundle identifier**: например `com.mycompany.myapp`
- **Display name**: например `Моё Приложение`
- **Package manager**: выбери pnpm, npm или yarn
- **Install dependencies**: Да/Нет

### Шаг 4: Запусти созданное приложение

```bash
cd МоёПриложение

# iOS (только на Mac)
pnpm run ios

# Android
pnpm run android
```

## 🎯 Что делает команда

Когда запускаешь `create-react-native-app МоёПриложение`:

1. ✅ Копирует все файлы из шаблона
2. ✅ Заменяет плейсхолдеры:
   - `HelloWorld` → `МоёПриложение`
   - `helloworld` → `моёприложение`
   - `com.helloworld` → твой bundle ID
3. ✅ Переименовывает папки:
   - `ios/HelloWorld/` → `ios/МоёПриложение/`
   - iOS проекты (.xcodeproj, .xcworkspace)
   - Android структура пакетов
4. ✅ Устанавливает зависимости
5. ✅ Устанавливает iOS pods (на Mac)
6. ✅ Инициализирует git
7. ✅ Показывает инструкции по настройке Firebase и Maps

## 🧪 Протестируй сейчас

```bash
# Быстрый тест (пропускает установку зависимостей)
create-react-native-app ТестоваяАппка --skip-install

# Или запусти автоматические тесты
cd /path/to/create-react-native-app
./test-cli.sh
```

## 📚 Документация

Я создал много документации:

- **[INDEX.md](INDEX.md)** - Индекс всех документов
- **[SUMMARY.md](SUMMARY.md)** - Полный обзор проекта (НАЧНИ ОТСЮДА!)
- **[GET_STARTED.md](GET_STARTED.md)** - Быстрый старт за 1 минуту
- **[QUICKSTART.md](QUICKSTART.md)** - Краткий справочник
- **[DEVELOPMENT.md](DEVELOPMENT.md)** - Подробное руководство разработчика
- **[README.md](README.md)** - Основной README (на английском)

### Для пользователей созданных приложений:
- **[template/SETUP.md](template/SETUP.md)** - Инструкции по настройке Firebase и Maps
- **[template/README.md](template/README.md)** - Документация приложения

## 🎨 Опции команды

```bash
# Базовое использование
create-react-native-app МоёПриложение

# С конкретным менеджером пакетов
create-react-native-app МоёПриложение --package-manager pnpm
create-react-native-app МоёПриложение -p npm

# Пропустить установку (быстрее для тестов)
create-react-native-app МоёПриложение --skip-install

# Пропустить git
create-react-native-app МоёПриложение --skip-git

# Интерактивный режим (спросит название)
create-react-native-app
```

## 📦 Публикация в npm

Когда будешь готов поделиться с миром:

```bash
# 1. Убедись что всё работает
./test-cli.sh

# 2. Войди в npm (только первый раз)
npm login

# 3. Опубликуй
npm publish
```

После публикации любой сможет использовать:

```bash
npx create-react-native-app ИхПриложение
```

## ⚠️ Важно для пользователей

После создания приложения нужно:

### 1. Добавить Firebase конфиги
- **Android**: `android/app/google-services.json`
- **iOS**: `ios/НазваниеПриложения/GoogleService-Info.plist`

### 2. Добавить Google Maps API ключ
- **Android**: в `android/local.properties` добавь `GOOGLE_MAPS_API_KEY=твой_ключ`
- **iOS**: уже настроен через CocoaPods

### 3. Установить зависимости (если пропустил)
```bash
pnpm install
cd ios && pod install && cd ..
```

## 🔧 Структура проекта

```
create-react-native-app/
├── bin/cli.js              # Точка входа
├── src/
│   ├── index.js           # Основная логика CLI
│   ├── prompts.js         # Интерактивные вопросы
│   ├── template.js        # Копирование и замена
│   └── utils.js           # Утилиты
├── template/              # Твой шаблон приложения
│   ├── android/           # Android код
│   ├── ios/               # iOS код
│   ├── assets/            # Шрифты, иконки
│   └── ...                # Все твои файлы
└── package.json           # Конфиг CLI
```

## 🎊 Следующие шаги

1. ✅ **Протестируй локально**
   ```bash
   npm install
   npm link
   create-react-native-app ТестАпп
   ```

2. ⏭️ **Исправь проблемы** (если найдёшь)

3. ⏭️ **Опубликуй в npm**
   ```bash
   npm publish
   ```

4. ⏭️ **Протестируй опубликованную версию**
   ```bash
   npx create-react-native-app ТестПослеПубликации
   ```

5. ⏭️ **Поделись с командой!**

## 💡 Советы

- Используй `--skip-install` для быстрого тестирования
- Протестируй с npm, yarn и pnpm перед публикацией
- Удали из template любые приватные ключи API
- Обновляй CHANGELOG.md при изменениях

## 🐛 Проблемы?

### "command not found: create-react-native-app"
```bash
# Перезапусти терминал после npm link
# Или проверь что npm global bin в PATH
npm bin -g
```

### Плейсхолдеры не заменяются
Проверь `src/template.js` - массив `filesToReplace`

### Проблемы с iOS
```bash
cd ios
pod install
```

### Проблемы с Android
```bash
cd android
./gradlew clean
```

## 📞 Нужна помощь?

Читай документы в таком порядке:
1. **[SUMMARY.md](SUMMARY.md)** - Обзор всего
2. **[GET_STARTED.md](GET_STARTED.md)** - Быстрый старт
3. **[DEVELOPMENT.md](DEVELOPMENT.md)** - Детальный гайд
4. **[template/SETUP.md](template/SETUP.md)** - Для пользователей приложений

## 🎉 Поздравляю!

Твой CLI инструмент готов! 

Попробуй прямо сейчас:

```bash
cd /path/to/create-react-native-app
npm install
npm link
cd ~/Desktop
create-react-native-app МоёПервоеПриложение
```

Удачи! 🚀

---

P.S. Не забудь прочитать [SUMMARY.md](SUMMARY.md) для полного понимания что и как работает!

